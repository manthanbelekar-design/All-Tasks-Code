 Task 2: Write python code to read text (Code)
import tkinter as tk
from tkinter import filedialog

def open_file():
    file_path = filedialog.askopenfilename(
        filetypes=[("Text Files", "*.txt")]
    )

    if file_path:
        with open(file_path, "r") as file:
            content = file.read()

        text_area.delete("1.0", tk.END)
        text_area.insert(tk.END, content)

root = tk.Tk()
root.title("Text File Reader")
root.geometry("500x400")

btn = tk.Button(root, text="Open Text File", command=open_file)
btn.pack(pady=10)

text_area = tk.Text(root)
text_area.pack(expand=True, fill="both")

root.mainloop()

Task 3: write code to display "jpg" file using cv2 (code)
import cv2

# Read JPG image
img = cv2.imread("image.jpg")

# Display image
cv2.imshow("JPG Image", img)

# Wait until a key is pressed
cv2.waitKey(0)

# Close window
cv2.destroyAllWindows()
